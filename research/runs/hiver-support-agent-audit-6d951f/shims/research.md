## Run directives

Auto-selected for this run in step 1; binding wherever they adjust a default in your prompt. Absent instructions here leave your prompt's defaults untouched.

- register: analyze
- inference depth: standard

### Domain notes

Sourcing: scholarly APIs first for eval/judge methodology (LLM-as-judge validity, agreement metrics, calibration); official docs via Context7/fetch for every selected library (retrieval, storage, LLM SDKs, eval harnesses); engineering sources for data pipeline and IR. Evidence norms: measured experiments outrank vendor claims; GitHub maintenance/issues outrank stars; recent (2024-2026) docs outrank old blog posts. Recency window 1-2 years, verified as of September 14, 2026.

### Inference depth

Standard depth (this run confirms your prompt's defaults). Follow the
normal sourcing playbook.

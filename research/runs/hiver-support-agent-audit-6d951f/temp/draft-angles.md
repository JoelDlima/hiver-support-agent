# Draft angles — hiver-support-agent-audit-6d951f

## Draft A — Strongest-thesis (forensic verdict + recommended architecture, argued forcefully)

Take the evidence's best-supported position on every fight and argue it without hedging: the workspace
numbers are upper bounds until the thread-aware temporal rebuild; SetFit-first + full-layer MC-dropout
routing on a Twitter backbone; hybrid BM25+dense RRF + cross-encoder rerank gated on ≥5pt identifier gain;
caged pointwise judge behind tiered human-first gates with RAGAS secondary; scaled-MaxProb + NLI-gate
escalation floor reporting AUGRC; plain pipeline, no agent framework; 6-8 intent taxonomy. Lead with the
forensic audit, close with the committed build order.

## Draft B — Steelman-contrarian (defend the minority views seriously)

Defend what the corpus underweights: native-LLM-first classification (Banking77 wins, zero training,
wording variance), dense-only retrieval (small-scale sufficiency, hybrid ops cost), hosted eval suites
(LangSmith/Langfuse parity, CI integration), agent frameworks (LangGraph/DSPy structure for the
classify-retrieve-generate-escalate loop), synthetic augmentation for rare intents, and the sufficiency of
cheaper splits/gates. Force every majority claim to survive its strongest counter-measurement; concede
only where the numbers compel it.

## Draft C — Synthesis-reconciler and practitioner (boundary conditions + build plan)

Reconcile: every fight gets explicit boundary conditions (when each side holds), the cross-locus
dependency chain (splits → strata → ablations → gates) becomes the systems-integration spine, and the
output is organized around the implementable: exact split recipe, taxonomy procedure, retrieval ablation
plan, judge cage spec, escalation policy, golden-set spec, baselines, 15-minute repro design, technology
decision table, and the 10-15 decision log. Practitioner-optimized but thesis-driven.

# UNIQUENESS — Our One-Page Wedge (2026-09-11)

**Wedge (1 line):** everyone ships a generic Apple/Amazon GPT wrapper that prints fluent text; we ship a **VirginTrains rail agent that proves where it loses** — dual golden with published flips + kappa ceilings, safety/money recall tables, open FAILs, Groq pinned behind a grounding gate (fail-closed to template keyless), all repro'd CPU-keyless in **2.3 min** (`README.md:60`).

**Why VirginTrains beats the 10,000th Apple/Amazon wrapper:** Amazon = 1 lookup (`order_status`, median reply 11.5 min); Apple = 10 diagnostic slices (median 71 min) — good, but now the expected pick. Virgin = Delay Repay money + amendment + timetable in one thread, where a wrong time/platform/price costs cash, not annoyance. Era signature (Oct–Nov 2017 burst 91%, auto-Delay-Repay failures documented Guardian Dec-2017 ×2, BBC-2016, Independent-2015: "automatic" refunds that never arrived, advance-only + online-direct + single-operator strings) makes it instantly recognizable and temporally falsifiable. Station names (euston 4,220 hits) are slots, never keywords; £/HH:MM tokens must be grounded or omitted (`src/groq_draft.py:validate_draft`). No Amazon demo can say that.

## 5 proofs (each with artifact, all inside .)

1. **Dual golden, flips published, ceilings disclosed.** Virgin weak-200 (keyword-circular, stratify-only) + human-200 (60 manual-style + 140 rulebook-assisted, **41/200 intent flips**, weak-vs-human acc 0.795 κ 0.772) — `evaluation/virgin/golden_v1.csv`, `golden_human_200.csv`, `SAMPLING_NOTE.md`, `BASELINE_VS_FINAL.md`. Headline is human-200: final 0.795/0.803 vs simple 0.790/0.796 — balanced retrain put final ahead on intent (small-n, ±0.07 CIs) and winning esc F1 **0.767 vs 0.238**. Literature bar (Rao & Callison-Burch 2606.00093: protocol alone swings accuracy 0.551→0.899, κ across zero; 21-judge Δκ 33–41pp deflation; Judge's Verdict Tier-1 κ 0.781–0.816) is why we report κ + confusion + protocol, never accuracy-alone.
2. **Safety recall + money recall as first-class outputs.** Virgin: safety 1.000 on n=3 (tiny — stated, not claimable alone) + **money recall 0.850 (17/20, the better-powered signal)** + esc 2×2 acc 0.915 κ 0.715 P 0.778 R 0.757 F1 0.767 (`evaluation/virgin/JUDGE_AGREEMENT.md`); Apple transfer reference: safety **0.909 (n=11)**. 4-trigger rail rules (SAFETY_ADDONS overcrowd/evacuate/stranded + money_threshold/money_review + human-request + unresolvable) with reason codes + handoff packet. Pre-fix single-threshold-on-confidence was the documented production bug — F6 names the mechanism (money gate conf<0.7 never fired when LogReg was confidently wrong at 0.746); virgin money rule now escalates refund/repay/compensation/reprint/receipt/£ words at ANY confidence.
3. **Misleading honesty as policy, not footnote.** Weak-200 0.975 labeled "CIRCULAR, do not use as headline" (was 1.000 pre-fix; the 5 misses ARE the fixed cases); groundedness 4.21/0.900 labeled template-shaped; no containment/CSAT/BLEU-as-proof anywhere (§9 rule). Air Canada precedent ($812 negligent-misrepresentation ruling, "separate legal entity" defense rejected) is why F2's no-guess-times behavior + £/HH:MM gate exist. A wrapper team ships the 0.975; we headline the 0.795 with the reason.
4. **Groq-gated: speed without liability.** `openai/gpt-oss-20b` ($0.075/$0.30 per M, ~1000 tok/s; strict:true constrained decoding; OpenAI-compatible) pinned temp-0 JSON-schema behind `validate_draft` + NLI-queue + wκ≥0.60/safety-recall≥0.90 ship gate — **fail-closed to template on no-key/no-client/validation-fail/error** (`src/groq_draft.py`, Instructor `from_provider("groq/openai/gpt-oss-20b")` with graceful fallback to JSON parsing; fallback `qwen/qwen3.8-27b` conditional preview-only). Zero LLM scores claimed keyless. Reviewers punish "we call GPT-4" (Google VP Mowry Feb-2026 check-engine light; 80%-die-by-EOY-2026 analyses; HN "thin prompt+UI gets crushed"); our LLM is never on the critical path.
5. **Repro 2.3 min CPU-keyless + failure suite as spec.** Full pipeline timed 2.3 min (`README.md:60`); virgin index 27,172 docs build 0.5s p50 7.1ms/p95 7.9ms; no vector DB, no agent framework, $0 marginal. Virgin F1–F7: **9 PASS / 0 PARTIAL / 0 FAIL of 9 probes** (was 5/1/3 pre-fix; fixes landed + regression-tested in `tests/test_virgin_fixes.py`) with residual lexicon gaps disclosed (`evaluation/virgin/FAILURE_TESTS.md`) — packed-vs-overcrowding lexicon shape, French-stranded cognate gap, repeat-refund calibration, lost-ticket→lost-property cascade (all fixed, residuals noted). Rubric signals reviewers score in 2025–2026 (Reskilll: "another chatbot / wrapping an API without adding value" = low; error handling + clear target user + live demo that works = high; USAII-2026: Responsible-AI risk+mitigation + where-humans-stay-central; Harmonydata: "can the judge run it?" = 15 pts) map 1:1 onto these artifacts.

## 3 honest weaknesses (no spin)

1. **Virgin human-200 is single-annotator AI-assisted, no inter-annotator κ yet; safety n=3 (CI ~0.4–1.0) cannot pass any ship gate alone; per-intent n≈14–29 → F1 CIs ±~20%.** Ship needs double-label + adjudication + time-split eval.
2. **Money recall 0.850 (17/20) + 9/9 probes still share one residual mechanism: high-confidence errors (0.68–0.86) defeated every conf-gated guardrail pre-fix.** Safety is lexicon-shaped, not meaning-shaped (fixed `packed` via keywords + `CROWD_REMAP_TOKENS`, but novel crowd phrasing outside the token list still mislabels safe-direction; FR `bloqué` missed). Fixes H4–H7 landed + regression-tested; residual lexicon gaps remain disclosed in `evaluation/virgin/FAILURE_TESTS.md`.
3. **Groq live path unrun (no key); groundedness heuristic is circular-by-design; latency is index-only on Virgin (Apple e2e p50 180ms is the transfer proxy); English-only v1.** Nothing here launches without V-EVAL week-2 (human retrain, hybrid retrieval, 75-pair judge-human study).

## 30-sec pitch

"Ten thousand teams will demo an Amazon GPT wrapper that invents tracking links fluently. We picked VirginTrains, where inventing a time, platform, or price costs a passenger money — so our agent is built to abstain instead of guess. Every reply carries passage IDs, every escalation carries a reason code, and our report leads with where we lose: money recall 0.85 (17/20), nine probes green with residual lexicon gaps disclosed, all fixes regression-tested. The LLM upgrade (gpt-oss-20b behind a grounding gate) fails closed to templates without a key. Clone it, run the whole pipeline in two minutes on CPU, and check our numbers — that's the wedge: proof that loses honestly beats fluency that never gets tested."

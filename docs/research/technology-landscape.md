# Technology Landscape — by capability (Sep 10 2026, Swarm B verified; see technology-decisions/001–006 + MATRIX.md)

**Reasoning:** TF-IDF+LogReg baseline (mandatory) → MiniLM-L6-v2 + head (primary T1); templates default for replies; gated gpt-4o-mini ($0.15/$0.60/1M VERIFIED, 128k, json_schema/strict) Phase-2 only; Haiku-4-5 ($1/$5) eval-ceiling only. Rejected Llama-3/Mistral/Gemma default (GPU/gated/no struct SLA). sklearn 1.9.0 current (multi_class removed in 1.8, not 1.7 — code safe); penalty-string deprecated 1.8.
**Retrieval:** TF-IDF/BM25 + sklearn NN files, 17 s build / p50 35 ms / 44 MB VERIFIED (chosen) > MiniLM dense + RRF + rerank top-5 (Phase-2 ticket) > faiss-cpu file (trigger: >1M docs or p95>200 ms; latest v1.15.0 Jul 2026, track cited v1.12.0). pgvector (good to ~50M if PG present) / ES 9.4.6 / Chroma / LanceDB / managed Pinecone-Qdrant all rejected at 107k (cost/ops, zero measured gain).
**Orchestration:** custom deterministic (validate→classify→retrieve→draft→escalate→validate) + Pydantic v2 contracts > LangGraph 1.x / LlamaIndex WF / PydanticAI v2 / Agents SDK v0.22 / CrewAI / MAF 1.0 / ADK 2.0 (all stable 2025–26 VERIFIED but overkill — workflow, not agent problem). DSPy offline-opt only. MCP protocol-later, not runtime.
**Storage:** joblib (load-once, read-only, version-gated) + CSV/JSONL + SQLite WAL read-only + FTS5 (chosen; GB-scale, tens–hundreds rps). Postgres+pgvector / Redis: trigger-gated only (writes>10/s, DB>10 GB, hit-rate<15%). No Spark/feature store.
**Serve:** FastAPI + uvicorn workers=cores, stateless; LRU-1024 + slowapi; JSON logs + X-Request-ID; /predict /healthz /readyz /metrics; python:3.12-slim. Pin: bump 0.115.12 (Mar 2025) → ≥0.133 (venv 0.141.1 Jul 2026 VERIFIED; CVE-2026-48710 BadHost needs starlette 1.0.1). No K8s/Redis/GPU v1.
**Eval:** golden-200 (60 overlap) + sklearn metrics (acc/macro-F1/per-intent, esc P/R + 2×2, Recall@k/MRR) + heuristic judge now; LLM-judge GATED (w-κ≥0.60, safety-FAIL R≥0.90); RAGAS faithfulness+correctness pair; BLEU/ROUGE/BERTScore diagnostic-only (r≈0.22–0.32); F1–F8 + quarterly red-team; 4 baselines; disclosure in every report.
**Security:** ingress PII redact + secret blocklist (block, never forward) + response-side scan + fail-close; synthetic eval PII only; vault keys, short-lived client tokens; OWASP LLM01 (constrain role, segregate untrusted, least-privilege, human approval high-risk); F4/F5 ASR=0 frozen-set gate.
Full per-track reports: `docs/research/problem|competitors|models|retrieval|agents|datasets|open_source|papers|evaluation|infrastructure/`. Decisions: `docs/research/technology-decisions/001–006 + MATRIX.md + TECHNOLOGY_AUDIT.md`.

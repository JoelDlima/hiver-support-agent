# Hiver VirginTrains Agent — Report (6 pages, V-APP)

> Primary brand is now **VirginTrains** (UK rail, Delay Repay + amendment + timetable). AppleSupport kept as v1 evidence + transfer proof. Virgin KB/index/brand-config built in Phase 1; Virgin golden-200 **measured (V-EVAL): `evaluation/virgin/golden_human_200.csv` — 60 manual-style + 140 rulebook-assisted, 41/200 intent flips, weak-vs-human acc 0.795 κ 0.772, single-annotator (no inter-annotator κ yet)**. Headline = human-200 §B, never weak-200 §A. All paths inside `C:\Hiver`.

## 1. Problem framing: what "good" means for Virgin, what we didn't build

Brand **VirginTrains** — 27,817 outbound / 37,444 inbound pool (union 65,346, 2.32% of TWCS 2.8M; Oct–Nov 2017 burst 91%). Good = acknowledge → concrete next step (Delay Repay operator-form routing / amendment buy-new+refund-old flow / live-departures check) → DM redirect with journey + date + booking ref; never invent times, platforms, or prices (£/HH:MM must be grounded in inbound or passages or omitted); station names (euston 4,220 / manchester 1,442 inbound hits) are slot entities, never keywords. Escalate with reason on: safety (overcrowd/evacuation/injury/stranded + base legal lexicon), money (delay_claim / ticket_change_refund / fare_ticketing below 0.70 + £/refund signal), human request / injection, unresolvable (low-conf <0.45 / no-grounding / link-only / non-English / vague+frustration), complaint-review (frustration + sensitive). Output JSON: brand, intent, confidence, draft_reply, passage IDs, decision, reason, draft_path (template|groq), groq_reason. **Not built:** actual refunds/compensation processing, live National Rail / operator backend, multilingual (non-English → other + escalate), post-2017 timetable knowledge, autonomous resolution, Groq live path without key (keyless default; Groq behind schema+gate, fail-closed to template). UI is demo-only (direct agent import); engine first. See `research/datasets/virgin_data_quality.md`, `evaluation/virgin/SAMPLING_NOTE.md`, `src/virgin_intents.py`.

## 2. Results vs two baselines

Golden **200 measured** (`evaluation/virgin/golden_human_200.csv`; `evaluation/virgin/SAMPLING_NOTE.md`): stratified from 37,444 inbound pool, 10 strata × ~20 (seed 7), 60 manual-style first + 140 rulebook-assisted; 41/200 intent flips, weak-vs-human acc 0.795 κ 0.772 (single annotator, no inter-annotator κ; n=200 → acc CIs ≈±0.07). Rare oversample accessibility 20/148 (~14% near-census) + 10 safety-probe extras, lost/howto 20 each (~6%), money-overlap dual-flag (refund spans delay↔amendment), context = current + ≤2 prior turns, 133 cross-brand agent rows excluded (+5 robustness extras). Systems: **trivial** (majority other_out_of_scope + canned, always auto), **simple** (virgin keyword first-match followup-last + top-1 copy), **final** (per-brand TF-IDF LogReg `models/intent_virgin.pkl` 30k weak + TF-IDF NN virgin index 27,172 docs 19,898 feats p50 7.1ms/p95 7.9ms + template + rail rules). CPU keyless. Full tables: `evaluation/virgin/BASELINE_VS_FINAL.md` (§A weak / §B human-headline / §C per-intent), raw: `results_weak200.csv`, `results_human200.csv`.

| set | system | intent acc / macroF1 | esc P/R/F1 | ground ≥4 |
|---|---|---|---|---|
| weak-200 Virgin (circular — labels = keyword rules, stratify only) | trivial / simple / final | 0.095/0.017 – **1.000/1.000 by construction** – 0.795/0.793 | 0.000/0.000/0.000 – 1.000/0.179/0.303 – 0.500/0.893/0.641 | 0.000 – 0.015 – 0.935 |
| human-200 Virgin (**HEADLINE**, single-annotator, 41 flips) | trivial / simple / final | 0.075/0.014 – 0.795/0.801 – **0.660/0.672** | 0.000/0.000/0.000 – 1.000/0.135/0.238 – **0.580/0.784/0.667** | 0.000 – 0.015 – 0.935 (mean 4.21) |
| transfer reference (Apple **measured**, same pipeline family) | trivial / simple / final on human-60 | 0.217 – 0.517 – 0.433 | 0.000 – 0.000 – **0.424** (P 0.368 R 0.500) | – – 1.000 |
| transfer reference (Apple human-200) | final | 0.660/0.65 | **0.582** (P 0.469 R 0.767) | 1.000 (4.83) |

Deltas Virgin human final−simple: intent acc −0.135, macroF1 −0.129, esc_F1 **+0.429** (weak: −0.205/−0.207/+0.338). Reading: simple reproduces weak labels that overlap human labels (0.795) so it wins intent; final pays an intent price for generalizing off 30k weak bootstraps (train-subset acc 0.949, circular) and is justified by escalation + groundedness. Per-intent F1 (human-200, simple→final): delay 0.870→0.791 (24), refund 0.762→0.682 (23), timetable 0.791→0.667 (21), lost 0.857→0.750 (16), complaint 0.714→0.596 (23), fare 0.947→0.774 (18), accessibility 0.848→**0.500** (14), howto 0.703→0.667 (17), followup 0.882→0.811 (15), other 0.640→0.486 (29) — small-n (±~20%/stratum), worst drop = accessibility tail.

Weak-label coverage on Virgin pool (non-other 16,892/37,444 = 45.1%: delay 4,003 / complaint 4,405 / followup 4,515 / amendment 1,657 / timetable 885 / fare 594 / howto 338 / lost 347 / accessibility 148 / other 20,552) is **stratification only**. Judge (`evaluation/judge.py` v1 heuristic + LLM hook): Virgin esc-vs-human acc 0.855 κ 0.577, safety recall **1.000 on n=3 (tiny — CI ~0.4–1.0, gate NOT claimable)**, money recall 0.800 (16/20, n=20 = better-powered signal); groundedness mean 4.28, ≥4 rate 1.000 (template-shaped, circular). LLM run needs key (pinned model, temp 0), else advisory-only; ship-gate wκ ≥0.60 + safety-recall ≥0.90. Virgin index ready: `data/indexes/virgin/` (27,172 docs, build 0.5s, p50 7.1ms). Harness: `scripts/run_eval.py`, `eval_human60.py`, `run_judge_agreement.py` (see `evaluation/virgin/JUDGE_AGREEMENT.md`). Re-verified 2026-09-11: `run_virgin_eval.py` + `run_virgin_judge.py` re-ran clean; numbers above match `results_human200.csv` + `BASELINE_VS_FINAL.md` exactly.

## 3. Failure analysis: top 5 Virgin (exact-text re-probe 2026-09-11 — `evaluation/virgin/FAILURE_TESTS.md` F1–F7, 9 probes: 7 PASS / 2 PARTIAL / 0 FAIL, no unsafe output; code has moved since the 2026-09-10 5/1/3 run — fixes noted below)

1. **Colloquial overcrowding intent gap remains, decision fixed — F4a now PARTIAL (was FAIL).** `Wow... this train from Oxford to Stockport is ridiculously packed @VirginTrains` → `other_out_of_scope | escalate | legal_safety | conf 0.825` (human: `complaint_service` + escalate). `packed` is now in SAFETY_ADDONS so the safety escalate fires, but `packed` is still not a complaint KEYWORD and the LogReg (trained on weak labels) still predicts `other` confidently. Contrast F4b PASS (`overcrowding` → `complaint_service | escalate | legal_safety`). Pool has 414 packed/rammed/crush/overcrowd hits — colloquial tail escalates but mislabels. Hypothesis H4 (still open): extend complaint KEYWORDS (`packed/rammed/crammed/crush`), F4a as regression probe.
2. **French stranded passenger fixed — F5 now PASS (was FAIL).** `bonjour, suite a des conneries de @VirginTrains je suis bloque en Angleterre…` → `other | escalate | unresolvable-language | conf 0.832` with triage draft (human: escalate `unresolvable`). Non-English gate now lives in the serving path (`has_non_english` → escalate). Pool: 6 FR/ES hits — small slice, high severity. Residual: FR `bloqué` ≠ `stranded` so safety addon still misses; multilingual safety cognates (`bloqué/varado/coincé`) remain H5 follow-up.
3. **Money-gate recalibrated — F6 now PASS (was FAIL, explains judge money recall 0.350→0.800).** `Why have i never recieved my refund on tickets… Almost 2 months now ??` → `ticket_change_refund | escalate | money_review | conf 0.744` (human: `money_threshold` escalate — repeat + 2 months). Fix measured: virgin money rule now escalates refund/repay/compensation/£ words at ANY confidence (confident money errors cost cash); plain delay questions without money words still auto-handle below. Re-run judge slice: money recall 0.800 (16/20). Residual H6: repeat/duration cues (`again/few times/months`) without money words can still slip; target money recall ≥0.90.
4. **Lost-ticket-with-receipt misrouted — F7 PARTIAL (safe text, wrong direction, unchanged).** `Hey @VirginTrains - I have lost my open ticket home. Can you reprint at the station? I have my receipt` → `lost_property | auto_handle | conf 0.686` → lost-on-train template (human: `ticket_change_refund` + money review — correct direction is station reissue, not lost-property office). Cause: `lost my` steers weak label + classifier + template; money gate then misses (`lost_property` ∉ money_intents) — intent miss cascades into decision miss. Hypothesis H7: amend cues (`reprint/receipt/at the station`) outrank `lost my` in serving order (already done in adjudication) + F7 regression probe.
5. **Timetable invention risk held + PII decision gap + wording fragility — F2 PASS (exact) / fragile variant noted / F3b PASS-with-note.** `is the 21:03 train from Euston to Birmingham International still running…?` exact (with `/ running on time`) → `timetable_platform | auto_handle | conf 0.916` → "I won't guess times or platforms here… Check live departures, then DM us…" — 21:03 NOT repeated/confirmed (anti-Trainline behavior); guard = `groq_draft.validate_draft` £/HH:MM grounding gate (H2). Fragility (SELF_REVIEW D5, confirmed 2026-09-11): bare `still running?` without `running on time` → `other | auto_handle | conf 0.896` — `still running` is not a timetable keyword. `Why dont you call them… My number is 07403630041` → `other | escalate | human_request` → triage "Don't share personal info", number NOT echoed — right decision but via `call me`, NOT via PII detection (templates have no PII slots so echo is structurally impossible; gap is decision-only — H3: phone/email regex → escalate). Station names (euston 4,220 / manchester 1,442 hits) are slot entities, never keywords; F1 delay template must not invent DR bands/times (H1: add DR30 bands + booking-ref ask, grounded to ORR).

## 4. What is misleading about my headline number? (mandatory)

- Any **1.000 weak-200** Virgin number is **circular**: weak labels ARE the keyword rules (`src/virgin_intents.py:KEYWORDS`), so simple scores 1.000/1.000 by construction and final's train-subset acc 0.949 is equally circular. Never cite weak-200 accuracy. True ceiling is weak-vs-human agreement (Virgin 0.795, κ 0.772 on 200; Apple 0.517, κ 0.465 on 60) — Virgin's higher overlap is why simple beats final on intent here too.
- Virgin **human-200 is single-annotator AI-assisted** (60 manual-style + 140 rulebook-assisted, 41 flips; no inter-annotator κ yet). n=200 → CIs ≈±0.07 on acc; per-intent F1 on ~14–29/stratum unstable (±~20% — e.g. accessibility 0.848→0.421). Citing Apple human numbers as Virgin results would be a transfer fallacy — Apple rows above are reference only.
- Groundedness 0.935 ≥4 (mean 4.21) rewards templates by design (≤280 chars, acknowledge→step→DM, no invented £/HH:MM), not human factuality — heuristic would score 4–5 even on F4a/F7 (right words, wrong decision). Safety recall 1.000 on **n=3** has CI ~0.4–1.0 (gate NOT claimable); money recall 0.800 on n=20 is the better-powered escalation signal (up from 0.350 pre-fix — the gate fix is real, not a metric trick).
- Latency (virgin index p50 7.1ms, Apple end-to-end p50 180ms) is dev-CPU, not prod; burst-month slice (post-2017-11-15) + thin pre-2017 tail shift results; 5/9 failure-PROBE pass rate flatters (F1/F2 pass on draft safety while riding keyword rails; F3a passes via over-escalation accident).
- Trust esc-vs-human κ 0.347 + money/safety slices + failure FAILs + flips, not a single accuracy. Full 200-human double-label + adjudication needed for ship.

## 5. What we'd do next with one more week

(1) Double-label Virgin 200 (2nd annotator + adjudication on top of current 41-flip single-annotator set, report inter-annotator κ + flips; time-split train ≤2017-11-15 / test >2017-11-15, burst-month slice). Train supervised virgin LogReg/MiniLM on human labels (fixes accessibility collapse 0.421 + F4a/F7 misses). (2) Hybrid BM25+MiniLM + rerank, recall@3 ≥0.85 on virgin KB. (3) **Groq live step (key required, fail-closed today):** set `GROQ_API_KEY` in env (never in code), client `OpenAI(api_key=os.environ["GROQ_API_KEY"], base_url="https://api.groq.com/openai/v1")`, model `llama-3.3-70b-versatile` (131k ctx, JSON mode), `response_format json_schema {draft_reply}` strict:false + code-side `validate_draft` (≤280 chars, £/HH:MM grounded or omitted) + NLI gate; run 75-pair judge-human study (wκ ≥0.60 + safety-recall ≥0.90 to ship, else advisory); pace for free-tier ~30 RPM, never on critical path — no-key/error/validation-fail → template (reason `no-key|no-client|validation-fail|error` in signals). (4) 2-turn thread context + language ID + PII redactor + Virgin brand token (`@VirginTrains→<BRAND-VIRGIN>`). (5) Cache classifier load, per-brand `/metrics` + rate-limit + Docker load test. Cost stays $0 CPU keyless; Groq path pay-per-token only when key set.

## 6. Decision log (15 + Virgin addendum, why)

Apple 15 in `docs/DECISION_LOG.md` (brand→Virgin addendum lines 16–20: why Virgin over Amazon/Apple-common, brand-agnostic dict, Groq-behind-gate, rail safety add-ons, money-intent escalation) + `docs/DECISION_LOG.md` pointer. Summary: VirginTrains over Apple-common (27,817 outbound, memorable, still >>200 golden, TWCS-strict); 10 Virgin intents message-level (threads drift, fan-out 9.3%); no vector DB (27k docs, TF-IDF 0.5s/p50 7.1ms); no agent framework (5 fixed steps); templates over free LLM keyless (zero invented times/prices); per-brand TF-IDF+LogReg (CPU <2min, weak ceiling disclosed); weak bootstrap stratified-only; English-only v1; 4-trigger + rail/money escalation (single threshold misses overcrowd + refund overlap); DM = triage; golden 200 = 60 manual + 140 assisted (pending); heuristic judge + gated LLM; dedup cap K=5 (27,817→27,172); stateless FastAPI per-brand `/metrics`, no K8s; headline = human, never weak-200.
Repo: `C:\Hiver` (also `docs/DECISION_LOG.md`, `docs/REPORT_6PAGE.md` Apple reference). Borrowed: sklearn/pandas/FastAPI/pytest/Streamlit, TWCS (CC BY-NC-SA 4.0), Banking77 (CC-BY-4.0 intent design only), Groq docs (base_url/JSON mode/rate limits), National Rail / Virgin Delay Repay pages for policy wording (no prices quoted in templates). All research sources in `research/research_log.md` (V-APP: 5 websearches + 1 DDG-blocked).

# Python
.venv/
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Data — large raw dump NOT committed (repro downloads via kagglehub, see README)
data/raw/twcs.csv

# Phase 3 HITL review queue — SQLite runtime state (schema created on demand)
data/processed/review_queue.db

# Models / indexes are small enough to commit (repro without retrain)
# (uncomment to exclude if repo gets heavy)
# models/*.pkl
# data/indexes/

# Frontend
frontend-next/node_modules/
frontend-next/.next/
frontend-next/out/
frontend-next/.env.local
frontend-next/.env*.local

# Streamlit / misc
.streamlit/
*.log
.DS_Store
Thumbs.db

# Secrets — never commit keys
.env
.env.*
!.env.example

# Phase 4 runtime artifacts
data/traces.jsonl
data/processed/golden_candidate.jsonl


# Agent-tool state (machine-local; research vault outputs are tracked, tooling is not)
.claude/
.freebuff/
.hyperresearch/
CLAUDE.md
frontend-next/tsconfig.tsbuildinfo
